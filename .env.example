# SIGPOL — Variáveis de Ambiente
# Copie para .env e ajuste os valores
# NUNCA use caracteres especiais como @, !, # nas senhas Docker

# Banco de dados (sem caracteres especiais)
DB_PASSWORD=sigpol2026

# JWT (pode ser qualquer string longa)
JWT_SECRET=sigpol_jwt_dev_2026_trocar_em_producao

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost
